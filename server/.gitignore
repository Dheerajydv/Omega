# .gitignore for a Node.js + TypeScript project

# Dependency directories
# These are installed by package managers and should not be in version control.
node_modules/
.pnp
.pnp.js

# TypeScript compilation output
# This directory contains the compiled JavaScript code.
# It should be generated on the server or during a build step.
dist/
build/
out/
*.tsbuildinfo

# Log files
# Logs are machine-specific and can become very large.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
# These files contain sensitive information like API keys and database credentials.
# NEVER commit them. A .env.example file should be committed instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*

# Test reports and coverage
# These are generated when running tests and can be regenerated at any time.
coverage/
.nyc_output/

# IDE and editor directories
# These are user-specific settings for development environments.
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Operating System files
# These are automatically generated by macOS, Windows, and Linux.
.DS_Store
Thumbs.db
ehthumbs.db

# Optional - Un-comment if you don't want to commit your lockfile
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Build artifacts
# Optional build-related files and directories.
.tmp
.npm
